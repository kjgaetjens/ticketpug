extends layout.pug

append topMenu

block bottomContent        
        form(
            action="./checkout"
            method="POST"
            )
            input(
                type="submit"
                value="submit"
                )

append bottomContent