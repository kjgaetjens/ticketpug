extends layout.pug

append topMenu

block topContent
    .topContainer
        ul
            a(href="/account") Settings
            a(href="/account/payment") Payment
            a(href="/account/history") History
        h2 Account Settings




block bottomContent
    .profile
        h2 Profile
        .userDisplayInfo
            h3 Username from user session 
