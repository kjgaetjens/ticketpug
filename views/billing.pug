extends layout.pug

append topMenu

block bottomContent        
        form(
            action="./billing"
            method="POST"
            )
            input(
                type="submit"
                value="submit"
                )

append bottomContent