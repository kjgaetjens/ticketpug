extends layout.pug

append topMenu

block topContent        
        form(
            action="./billing"
            method="POST"
            )
            input(
                type="submit"
                value="submit"
                )

append bottomContent